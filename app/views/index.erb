<!doctype html>

<html lang="en">

<body>
  <h1>Articles</h1>

  <ul>
  <% @articles.each do |article| %>
      <p><%=article.title %></p>
      <p><%=article.content %></p>
  <% end %>
  </ul>
</body>
</html>
